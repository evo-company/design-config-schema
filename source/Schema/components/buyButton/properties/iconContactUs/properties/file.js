import definitions from 'Schema/definitions';


const NAME = 'file';


export default {
    [NAME]: Object.assign({
        title: NAME,
        description: 'SVG icon for "contact us" button',
    }, definitions.file)
};
